# Long-term rate of population change using sigmasq# from slope estimate changes in time# # Filename: ltpgr.R# Author: Darren Kavanagh# Created: 29.07.2009# Last Modified:# ======================================================# # mu (µ): determines how quickly the mean increases# sigmasq σ2: determines how quickly the variance in the normal#               distribution increases from slope method# tsl: time series length# L: number of N's to add together to make runsumltpgr <- function (mu, sigmasq, tsl, L) {	# load functions	source('dfslp.R')		lambda <- exp(mu)	    		    # confidence levels (Holmes & Fagan, 2002)    degfr <- dfslp(tsl, L) # degrees of freedom		lambdal95 <- exp(mu + qt(.025,degfr)*sqrt(sigmasq/(tsl-L))) 	lambdah95 <- exp(mu - qt(.025,degfr)*sqrt(sigmasq/(tsl-L)))		return(data.frame(lambda,lambdal95,lambdah95))}		